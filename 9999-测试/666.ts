/**
 * 解析文本加减乘除
 * @param text
 * @returns
 * @example parseText('1+2*3') => 7
 */
export function parseText(text: string): string {
  // 1. 解析文本中的变量
  // 2. 解析文本中的运算
  // 3. 返回解析后的文本
  return text
}
